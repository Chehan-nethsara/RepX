<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.repx">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".dealerprofile"></activity>
        <activity android:name=".dealer"></activity>

        <activity android:name=".Popup"></activity>
        <activity android:name=".product"></activity>

        <activity android:name=".AddSales"></activity>
        <activity android:name=".Customer_Details"></activity>
        <activity android:name=".EditShops"></activity>

        <activity android:name=".Login"></activity>
        <activity android:name=".SalesMenu"></activity>
        <activity android:name=".ShopRegister"></activity>
        <activity android:name=".Signup"></activity>
        <activity android:name=".UserProfile"></activity>
        <activity android:name=".ViewSale"></activity>


            <activity android:name=".add_product"></activity>
        <activity android:name=".view_product"></activity>
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>